using UnityEngine;

public class SpawnSet : MonoBehaviour
{
    [SerializeField] private SpawnListSO spawnListSO;

    private void SpawnTrm()
    {
        // foreach (SpawnSO spawn in spawnListSO.SpawnTrm)
        // {

        // }
    }
}
